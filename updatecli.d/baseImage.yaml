---
sources:
  tagFromApi:
    kind: shell
    spec:
      command: ./hack/getLatestTagFromApi.sh
targets:
  update:
    name: "Update base image to latest version"
    kind: dockerfile
    spec:
      file: Dockerfile
      instruction:
        keyword: "FROM"
        matcher: "registry.access.redhat.com/ubi8/ubi-minimal"
scms:
  origin:
    kind: "github"
    spec:
      branch: "main"
      user: "it-reaktion"
      email: "bot@updatecli.io"
      owner: "radiorabe"
      repository: "container-image-ubi8-minimal"
      token: '{{ requiredEnv "GITHUB_TOKEN" }}'
      username: '{{ requiredEnv "GITHUB_ACTOR" }}'
pullrequests:
  default:
    kind: "github"
    scmID: "origin"
    targets:
      - "update"
    spec:
      automerge: true
      labels:
        - "dependencies"
        - "docker"
